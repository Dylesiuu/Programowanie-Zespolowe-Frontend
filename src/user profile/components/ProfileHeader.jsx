import React from "react";
import styles from "../Profile.module.css";

const ProfileHeader = ({ user }) => {
  return <div className={styles.profileHeader}>Profil</div>;
};

export default ProfileHeader;
